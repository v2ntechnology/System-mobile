# URL base da API. Enquanto o backend não existe, o app usa os mocks de `src/mocks`.
VITE_API_BASE_URL=http://localhost:8080/v1

# "mock" (padrão nesta fase) | "http" (quando o backend estiver plugado)
VITE_API_MODE=mock
