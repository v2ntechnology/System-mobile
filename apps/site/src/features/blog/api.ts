/** Única fronteira do blog. Hoje: `src/mocks/posts.ts`; amanhã, o CMS. */
export { fetchPosts, fetchPost, listPostSlugs } from "@/mocks/posts";

export type { Post, PostSummary, PostBlock } from "@/mocks/posts";
